export const MOCK_LOCALS = ['Guichê', 'Sala', 'Recepção', 'Consultório', 'Ambulatório']
export const MOCK_POSITIONS = Array(30)
  .fill(null)
  .map((_, index) => (index + 1).toString().padStart(2, '0'))
